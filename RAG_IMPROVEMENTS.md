# Улучшения RAG с цитированием источников - День 19

## Проблема
При первом тестировании команды `/testRagCitations` модель qwen3:4b отвечала "Информация не найдена в предоставленной документации", даже когда были найдены 5 релевантных чанков со средней релевантностью 79.88%.

### Причина
1. Системный промпт был недостаточно директивным
2. Форматирование контекста не акцентировало важность использования предоставленных фрагментов
3. Модель не понимала, что фрагменты ОБЯЗАТЕЛЬНО нужно использовать

## Решение

### 1. Улучшенный системный промпт (Main.kt:86-138)

**Было:**
```kotlin
val RagWithCitationsRole = """
Ты — AI ассистент, который отвечает на вопросы на основе предоставленной документации.

КРИТИЧЕСКИ ВАЖНО:
1. КАЖДОЕ утверждение в твоем ответе ДОЛЖНО содержать ссылку на источник
2. Формат ссылки: [источник: <имя_файла>, строки <начало>-<конец>]
...
"""
```

**Стало:**
```kotlin
val RagWithCitationsRole = """
Ты — AI ассистент, который ОБЯЗАН отвечать на вопросы ТОЛЬКО на основе предоставленных фрагментов документации.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
АБСОЛЮТНЫЕ ПРАВИЛА (НАРУШЕНИЕ ЗАПРЕЩЕНО):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. ИСПОЛЬЗУЙ ТОЛЬКО ПРЕДОСТАВЛЕННЫЕ ФРАГМЕНТЫ
   - Читай каждый фрагмент внимательно
   - Если ответ есть в фрагменте - ОБЯЗАТЕЛЬНО используй его
   - НЕ игнорируй фрагменты!

2. КАЖДЫЙ ФАКТ = ЦИТАТА
   - Формат: [источник: filename, строки X-Y]
   - После КАЖДОГО утверждения - цитата
   - Минимум 1 цитата в ответе

3. ЗАПРЕЩЕНО:
   ❌ Отвечать без цитат
   ❌ Придумывать факты
   ❌ Писать "информация не найдена", если она ЕСТЬ в фрагментах
...
```

**Ключевые улучшения:**
- Более императивный тон ("ОБЯЗАН", "ЗАПРЕЩЕНО")
- Визуальное выделение с помощью Unicode символов
- Few-shot примеры правильных и неправильных ответов
- Пошаговый алгоритм ответа

### 2. Улучшенное форматирование контекста (RagService.kt:167-226)

**Было:**
```kotlin
appendLine("=== РЕЛЕВАНТНАЯ ИНФОРМАЦИЯ ИЗ ДОКУМЕНТАЦИИ ===\n")
appendLine("ВАЖНО: При ответе ОБЯЗАТЕЛЬНО используйте цитаты...")
```

**Стало:**
```kotlin
appendLine("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
appendLine("ДОКУМЕНТАЦИЯ ДЛЯ ОТВЕТА НА ВОПРОС")
appendLine("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
appendLine()
appendLine("🔴 КРИТИЧЕСКИ ВАЖНО:")
appendLine("1. Эти фрагменты РЕЛЕВАНТНЫ вопросу")
appendLine("2. ОБЯЗАТЕЛЬНО используй информацию из них")
appendLine("3. Каждый факт ДОЛЖЕН иметь цитату")
```

**Для каждого фрагмента:**
```kotlin
appendLine("━━━ ФРАГМЕНТ ${i + 1} ━━━")
appendLine("📍 Локация: ${record.sourceFile}, строки ${record.startLine}-${record.endLine}")
appendLine("🎯 Релевантность: %.2f%% (высокая)".format(relevance * 100))
appendLine("📎 Используй цитату: [источник: ${record.sourceFile}, строки ${record.startLine}-${record.endLine}]")
appendLine()
appendLine("📄 СОДЕРЖАНИЕ:")
appendLine(record.text)
```

**После всех фрагментов:**
```kotlin
appendLine("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
appendLine("ИНСТРУКЦИЯ ПО ОТВЕТУ:")
appendLine("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
appendLine()
appendLine("✅ ИСПОЛЬЗУЙ информацию из фрагментов выше")
appendLine("✅ ДОБАВЬ цитату после каждого факта")
appendLine("✅ ФОРМАТ цитаты: [источник: файл, строки X-Y]")
appendLine()
appendLine("❌ НЕ пиши 'информация не найдена', если она ЕСТЬ выше!")
appendLine("❌ НЕ придумывай факты - используй ТОЛЬКО предоставленные фрагменты!")
```

**Ключевые улучшения:**
- Визуальное выделение с эмодзи (🔴, 📍, 🎯, 📎, 📄, ✅, ❌)
- Явное указание релевантности каждого фрагмента
- Готовый формат цитаты для копирования
- Повторение инструкций в конце контекста

## Как протестировать улучшения

### Шаг 1: Убедитесь, что Ollama запущена
```bash
ollama list
# Должны быть: nomic-embed-text, qwen3:4b
```

Если qwen3:4b нет:
```bash
ollama pull qwen3:4b
```

### Шаг 2: Пересоздайте embeddings с метаданными
```bash
# В Telegram боте
/createEmbeddings
```

Это обновит embeddings с информацией о номерах строк.

### Шаг 3: Настройте порог релевантности (опционально)
```bash
# Рекомендуется средний порог
/setThreshold 0.5
```

### Шаг 4: Протестируйте команду
```bash
/testRagCitations как установить порог релевантности?
```

### Ожидаемый результат
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📖 RAG С ЦИТИРОВАНИЕМ ИСТОЧНИКОВ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 Вопрос: как установить порог релевантности?

━━━ ОТВЕТ ━━━
Для установки порога релевантности используйте команду /setThreshold <значение>
[источник: readme.md, строки 914-945].

Диапазон значений: 0.0 - 1.0 [источник: readme.md, строки 930-936].

Рекомендации по настройке:
- 0.3-0.4 (низкий) - больше результатов, может включать шум [источник: readme.md, строки 932-933]
- 0.5-0.6 (средний) - сбалансированный подход (рекомендуется) [источник: readme.md, строки 934-935]
- 0.7-0.8 (высокий) - только высоко релевантные чанки [источник: readme.md, строки 936-937]

━━━ СТАТИСТИКА ━━━
📊 Использовано чанков: 5
📎 Найдено цитат: 4
✅ Порог релевантности: 0.5
📈 Средняя релевантность: 79,88%

✅ Ответ содержит 4 ссылок на источники
```

## Тестовые вопросы (5 вопросов из RAG_CITATIONS_TEST.md)

1. **Какие MCP серверы используются в проекте?**
   - Ожидается: список серверов с цитатами

2. **Какая модель используется для embeddings?**
   - Ожидается: nomic-embed-text с цитатой

3. **Какие команды бота связаны с RAG?**
   - Ожидается: список команд с цитатами

4. **Какой порог релевантности используется по умолчанию?**
   - Ожидается: 0.5 (50%) с цитатой

5. **Как работает автоматическое суммирование истории сообщений?**
   - Ожидается: лимит 20 сообщений, температура 0.0, лимит 3000 символов с цитатами

## Метрики успеха

### До улучшений:
- ❌ 0% ответов с цитатами (0 из 1 теста)
- ❌ Модель игнорировала предоставленные фрагменты
- ❌ Ответ "Информация не найдена", хотя информация была

### После улучшений (ожидаемые результаты):
- ✅ 100% ответов с цитатами (5 из 5 вопросов)
- ✅ Среднее 2-4 цитаты на ответ
- ✅ 0 галлюцинаций (все факты из документации)
- ✅ Высокая точность ссылок (±5 строк)

## Технические детали

### Изменённые файлы:
1. `Main.kt` - улучшенный системный промпт `RagWithCitationsRole`
2. `RagService.kt` - улучшенный метод `formatContextWithCitations()`

### Размер изменений:
- Системный промпт: с ~400 до ~900 символов (увеличение на 125%)
- Форматирование контекста: добавлено ~300 символов инструкций

### Влияние на производительность:
- Незначительное увеличение размера промпта (< 500 токенов)
- Время генерации: практически без изменений
- Качество ответов: ожидается значительное улучшение

## Если всё ещё не работает

### Вариант 1: Попробуйте другую модель
```bash
# Вместо qwen3:4b попробуйте:
ollama pull mistral
# или
ollama pull llama3.2:3b
```

Затем измените в `OllamaClient.kt:28`:
```kotlin
model = "mistral"  // или "llama3.2:3b"
```

### Вариант 2: Снизьте порог релевантности
```bash
/setThreshold 0.3
```

Это включит больше чанков, увеличив шансы найти нужную информацию.

### Вариант 3: Проверьте, что embeddings актуальны
```bash
# Пересоздайте embeddings
/createEmbeddings

# Затем повторите тест
/testRagCitations как установить порог релевантности?
```

## Выводы

Улучшения направлены на:
1. **Более директивный системный промпт** с явными примерами и запретами
2. **Визуально выделенный контекст** с эмодзи и форматированием
3. **Повторение инструкций** в конце контекста для закрепления
4. **Готовые форматы цитат** для упрощения работы модели

Эти изменения должны значительно повысить вероятность того, что модель будет:
- Использовать предоставленные фрагменты
- Добавлять цитаты к каждому факту
- Не отвечать "информация не найдена", если она есть

Проверьте результаты и дайте обратную связь!
