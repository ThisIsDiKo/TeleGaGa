# Возврат на llama3.2:1b и отладка RAG

## Изменения

### 1. Возврат на llama3.2:1b (OllamaClient.kt:28)

**Причина:** qwen3:4b работает слишком медленно (150-200 секунд на ответ)

**Было:**
```kotlin
model = "qwen3:4b"
```

**Стало:**
```kotlin
model = "llama3.2:1b"
```

### 2. Добавлен отладочный вывод (TelegramBotService.kt:666-676)

Добавлен debug-блок для проверки того, что передается модели:

```kotlin
// DEBUG: Выводим промпт в консоль
println("=== RAG PROMPT DEBUG ===")
println("System role length: ${systemRole.length}")
println("RAG prompt length: ${ragPrompt.length}")
println("Context preview (first 500 chars):")
println(contextWithCitations.take(500))
println("======================")
```

Это позволит увидеть в консоли бота:
- Длину системного промпта
- Длину RAG промпта
- Первые 500 символов контекста (для проверки, что чанки там есть)

## Проблема, которую мы диагностируем

Вы видите в ответе:
```
📊 Использовано чанков: 5
📈 Средняя релевантность: 79,88%
```

Но модель отвечает "нет информации".

### Возможные причины:

1. **Чанки не передаются** - контекст пустой или неполный
2. **Модель игнорирует контекст** - llama3.2:1b слишком слабая
3. **Неправильное форматирование** - модель не понимает формат

## Как протестировать

### Шаг 1: Перезапустите бота

```bash
# Остановите текущий бот (Ctrl+C)
# Запустите снова
export JAVA_HOME=/Users/dmitriikonovalov/Library/Java/JavaVirtualMachines/openjdk-17.0.1/Contents/Home
./gradlew run
```

### Шаг 2: Отправьте тестовый вопрос

```bash
/testRagCitations как установить порог релевантности?
```

### Шаг 3: Проверьте консоль бота

Вы должны увидеть что-то вроде:

```
=== RAG PROMPT DEBUG ===
System role length: 412
RAG prompt length: 2847
Context preview (first 500 chars):
=== ДОКУМЕНТАЦИЯ ДЛЯ ОТВЕТА ===

ВАЖНО:
- Эти фрагменты релевантны вопросу
- ОБЯЗАТЕЛЬНО используй информацию из них
- Каждый факт должен иметь цитату

--- Фрагмент 1 ---
Источник: readme.md, строки 914-945
Релевантность: 82.3%
Цитата: [источник: readme.md, строки 914-945]

### /setThreshold <0.0-1.0> (НОВИНКА - День 18)

Настраивает порог релевантности для RAG фильтрации.

**Использование:**

**Показать текущий порог:**
```
/setThreshold
```
...
======================
```

### Шаг 4: Проанализируйте вывод

**Если в "Context preview" видите фрагменты документации** - значит чанки передаются правильно, и проблема в модели.

**Если в "Context preview" НЕ видите фрагменты** - значит проблема в formatContextWithCitations().

## Что делать, если чанки передаются, но модель их игнорирует

### Вариант 1: Используйте более умную модель

llama3.2:1b - очень маленькая модель, которая может не справляться со сложными инструкциями.

Попробуйте более мощные модели:

```bash
# Вариант 1: llama3.2:3b (в 3 раза крупнее)
ollama pull llama3.2:3b

# Вариант 2: mistral (7B параметров, очень качественная)
ollama pull mistral

# Вариант 3: qwen2.5:3b (хороший баланс)
ollama pull qwen2.5:3b
```

Затем измените в OllamaClient.kt:28:
```kotlin
model = "llama3.2:3b"  // или "mistral" или "qwen2.5:3b"
```

### Вариант 2: Упростите системный промпт еще больше

Текущий промпт из Main.kt:86-117 (412 символов) можно упростить до:

```kotlin
val RagWithCitationsRole = """
Ты отвечаешь на вопросы ТОЛЬКО на основе фрагментов документации.

ПРАВИЛА:
1. Используй информацию из фрагментов
2. Каждый факт - цитата формата [источник: файл, строки X-Y]
3. Запрещено: придумывать факты, отвечать без цитат

ПРИМЕР:
Вопрос: "Какая команда?"
Ответ: "Команда /test [источник: readme.md, строки 100-105]."
""".trimIndent()
```

### Вариант 3: Уменьшите количество чанков

В TelegramBotService.kt:637 измените topK:

```kotlin
val searchResult = ragService.findRelevantChunksWithFilter(
    query, "readme", 3, threshold  // было 5, стало 3
)
```

Меньше чанков = проще для модели обработать.

### Вариант 4: Увеличьте порог релевантности

```bash
/setThreshold 0.8
```

Это вернет только самые релевантные чанки (1-2 вместо 5).

## Рекомендации

### Для быстрой работы:
- **llama3.2:1b** (текущая) - очень быстрая, но может игнорировать инструкции
- **llama3.2:3b** - хороший баланс скорости и качества

### Для качественных ответов с цитатами:
- **mistral** (7B) - лучшее качество, следует инструкциям
- **qwen2.5:3b** - отличный баланс

## Следующие шаги

1. Запустите бота и отправьте тестовый вопрос
2. Проверьте консоль - есть ли фрагменты в "Context preview"?
3. Если фрагменты есть, но модель их игнорирует - попробуйте другую модель
4. Дайте обратную связь - что вы видите в консоли?

Проект собран и готов к тестированию!
